export NVM_DIR="$HOME/.nvm"

if [ -s "$NVM_DIR/nvm.sh" ]; then
	. "$NVM_DIR/nvm.sh"
fi

if ! command -v node >/dev/null 2>&1; then
	echo "husky - node is required for commit hooks but was not found in PATH"
	exit 127
fi

npx --no -- commitlint --edit "$1"
